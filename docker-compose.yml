services:
  backend:
    build:
      context: ./scraper
      dockerfile: Dockerfile
    container_name: optimi-backend
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - ./scraper/.env
    restart: unless-stopped